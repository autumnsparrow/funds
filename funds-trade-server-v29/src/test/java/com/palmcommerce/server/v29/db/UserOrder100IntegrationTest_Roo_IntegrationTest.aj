// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserOrder100;
import com.palmcommerce.server.v29.db.UserOrder100DataOnDemand;
import com.palmcommerce.server.v29.db.UserOrder100IntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserOrder100IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserOrder100IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserOrder100IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserOrder100IntegrationTest: @Transactional;
    
    @Autowired
    UserOrder100DataOnDemand UserOrder100IntegrationTest.dod;
    
    @Test
    public void UserOrder100IntegrationTest.testCountUserOrder100s() {
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to initialize correctly", dod.getRandomUserOrder100());
        long count = UserOrder100.countUserOrder100s();
        Assert.assertTrue("Counter for 'UserOrder100' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserOrder100IntegrationTest.testFindUserOrder100() {
        UserOrder100 obj = dod.getRandomUserOrder100();
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to provide an identifier", id);
        obj = UserOrder100.findUserOrder100(id);
        Assert.assertNotNull("Find method for 'UserOrder100' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserOrder100' returned the incorrect identifier", id, obj.getOrderId());
    }
    
    @Test
    public void UserOrder100IntegrationTest.testFindAllUserOrder100s() {
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to initialize correctly", dod.getRandomUserOrder100());
        long count = UserOrder100.countUserOrder100s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserOrder100', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserOrder100> result = UserOrder100.findAllUserOrder100s();
        Assert.assertNotNull("Find all method for 'UserOrder100' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserOrder100' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserOrder100IntegrationTest.testFindUserOrder100Entries() {
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to initialize correctly", dod.getRandomUserOrder100());
        long count = UserOrder100.countUserOrder100s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserOrder100> result = UserOrder100.findUserOrder100Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserOrder100' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserOrder100' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserOrder100IntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to initialize correctly", dod.getRandomUserOrder100());
        UserOrder100 obj = dod.getNewTransientUserOrder100(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserOrder100' identifier to be null", obj.getOrderId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserOrder100' identifier to no longer be null", obj.getOrderId());
    }
    
    @Test
    public void UserOrder100IntegrationTest.testRemove() {
        UserOrder100 obj = dod.getRandomUserOrder100();
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder100' failed to provide an identifier", id);
        obj = UserOrder100.findUserOrder100(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserOrder100' with identifier '" + id + "'", UserOrder100.findUserOrder100(id));
    }
    
}
