// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserOrder101;
import com.palmcommerce.server.v29.db.UserOrder101DataOnDemand;
import com.palmcommerce.server.v29.db.UserOrder101IntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserOrder101IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserOrder101IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserOrder101IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserOrder101IntegrationTest: @Transactional;
    
    @Autowired
    UserOrder101DataOnDemand UserOrder101IntegrationTest.dod;
    
    @Test
    public void UserOrder101IntegrationTest.testCountUserOrder101s() {
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to initialize correctly", dod.getRandomUserOrder101());
        long count = UserOrder101.countUserOrder101s();
        Assert.assertTrue("Counter for 'UserOrder101' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserOrder101IntegrationTest.testFindUserOrder101() {
        UserOrder101 obj = dod.getRandomUserOrder101();
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to provide an identifier", id);
        obj = UserOrder101.findUserOrder101(id);
        Assert.assertNotNull("Find method for 'UserOrder101' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserOrder101' returned the incorrect identifier", id, obj.getOrderId());
    }
    
    @Test
    public void UserOrder101IntegrationTest.testFindAllUserOrder101s() {
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to initialize correctly", dod.getRandomUserOrder101());
        long count = UserOrder101.countUserOrder101s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserOrder101', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserOrder101> result = UserOrder101.findAllUserOrder101s();
        Assert.assertNotNull("Find all method for 'UserOrder101' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserOrder101' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserOrder101IntegrationTest.testFindUserOrder101Entries() {
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to initialize correctly", dod.getRandomUserOrder101());
        long count = UserOrder101.countUserOrder101s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserOrder101> result = UserOrder101.findUserOrder101Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserOrder101' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserOrder101' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserOrder101IntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to initialize correctly", dod.getRandomUserOrder101());
        UserOrder101 obj = dod.getNewTransientUserOrder101(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserOrder101' identifier to be null", obj.getOrderId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserOrder101' identifier to no longer be null", obj.getOrderId());
    }
    
    @Test
    public void UserOrder101IntegrationTest.testRemove() {
        UserOrder101 obj = dod.getRandomUserOrder101();
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder101' failed to provide an identifier", id);
        obj = UserOrder101.findUserOrder101(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserOrder101' with identifier '" + id + "'", UserOrder101.findUserOrder101(id));
    }
    
}
