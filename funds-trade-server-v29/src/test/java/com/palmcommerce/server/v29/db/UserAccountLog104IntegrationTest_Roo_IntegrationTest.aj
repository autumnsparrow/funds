// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountLog104DataOnDemand;
import com.palmcommerce.server.v29.db.UserAccountLog104IntegrationTest;
import com.palmcommerce.server.v29.service.UserAccountLogService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAccountLog104IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAccountLog104IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAccountLog104IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAccountLog104IntegrationTest: @Transactional;
    
    @Autowired
    UserAccountLog104DataOnDemand UserAccountLog104IntegrationTest.dod;
    
    @Autowired
    UserAccountLogService UserAccountLog104IntegrationTest.userAccountLogService;
    
    @Test
    public void UserAccountLog104IntegrationTest.testCountAllUserAccountLog104s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to initialize correctly", dod.getRandomUserAccountLog104());
        long count = userAccountLogService.countAllUserAccountLog104s();
        Assert.assertTrue("Counter for 'UserAccountLog104' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAccountLog104IntegrationTest.testFindUserAccountLog104() {
        UserAccountLog104 obj = dod.getRandomUserAccountLog104();
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog104(id);
        Assert.assertNotNull("Find method for 'UserAccountLog104' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAccountLog104' returned the incorrect identifier", id, obj.getLogId());
    }
    
    @Test
    public void UserAccountLog104IntegrationTest.testFindAllUserAccountLog104s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to initialize correctly", dod.getRandomUserAccountLog104());
        long count = userAccountLogService.countAllUserAccountLog104s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAccountLog104', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAccountLog104> result = userAccountLogService.findAllUserAccountLog104s();
        Assert.assertNotNull("Find all method for 'UserAccountLog104' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAccountLog104' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAccountLog104IntegrationTest.testFindUserAccountLog104Entries() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to initialize correctly", dod.getRandomUserAccountLog104());
        long count = userAccountLogService.countAllUserAccountLog104s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAccountLog104> result = userAccountLogService.findUserAccountLog104Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAccountLog104' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAccountLog104' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAccountLog104IntegrationTest.testSaveUserAccountLog104() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to initialize correctly", dod.getRandomUserAccountLog104());
        UserAccountLog104 obj = dod.getNewTransientUserAccountLog104(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserAccountLog104' identifier to be null", obj.getLogId());
        userAccountLogService.saveUserAccountLog104(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserAccountLog104' identifier to no longer be null", obj.getLogId());
    }
    
    @Test
    public void UserAccountLog104IntegrationTest.testDeleteUserAccountLog104() {
        UserAccountLog104 obj = dod.getRandomUserAccountLog104();
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog104' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog104(id);
        userAccountLogService.deleteUserAccountLog104(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAccountLog104' with identifier '" + id + "'", userAccountLogService.findUserAccountLog104(id));
    }
    
}
