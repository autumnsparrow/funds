// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserEncashStat;
import com.palmcommerce.server.v29.db.UserEncashStatDataOnDemand;
import com.palmcommerce.server.v29.db.UserEncashStatIntegrationTest;
import java.math.BigDecimal;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserEncashStatIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserEncashStatIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserEncashStatIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserEncashStatIntegrationTest: @Transactional;
    
    @Autowired
    UserEncashStatDataOnDemand UserEncashStatIntegrationTest.dod;
    
    @Test
    public void UserEncashStatIntegrationTest.testCountUserEncashStats() {
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to initialize correctly", dod.getRandomUserEncashStat());
        long count = UserEncashStat.countUserEncashStats();
        Assert.assertTrue("Counter for 'UserEncashStat' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserEncashStatIntegrationTest.testFindUserEncashStat() {
        UserEncashStat obj = dod.getRandomUserEncashStat();
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to initialize correctly", obj);
        BigDecimal id = obj.getStatId();
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to provide an identifier", id);
        obj = UserEncashStat.findUserEncashStat(id);
        Assert.assertNotNull("Find method for 'UserEncashStat' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserEncashStat' returned the incorrect identifier", id, obj.getStatId());
    }
    
    @Test
    public void UserEncashStatIntegrationTest.testFindAllUserEncashStats() {
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to initialize correctly", dod.getRandomUserEncashStat());
        long count = UserEncashStat.countUserEncashStats();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserEncashStat', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserEncashStat> result = UserEncashStat.findAllUserEncashStats();
        Assert.assertNotNull("Find all method for 'UserEncashStat' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserEncashStat' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserEncashStatIntegrationTest.testFindUserEncashStatEntries() {
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to initialize correctly", dod.getRandomUserEncashStat());
        long count = UserEncashStat.countUserEncashStats();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserEncashStat> result = UserEncashStat.findUserEncashStatEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserEncashStat' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserEncashStat' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserEncashStatIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to initialize correctly", dod.getRandomUserEncashStat());
        UserEncashStat obj = dod.getNewTransientUserEncashStat(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserEncashStat' identifier to be null", obj.getStatId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserEncashStat' identifier to no longer be null", obj.getStatId());
    }
    
    @Test
    public void UserEncashStatIntegrationTest.testRemove() {
        UserEncashStat obj = dod.getRandomUserEncashStat();
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to initialize correctly", obj);
        BigDecimal id = obj.getStatId();
        Assert.assertNotNull("Data on demand for 'UserEncashStat' failed to provide an identifier", id);
        obj = UserEncashStat.findUserEncashStat(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserEncashStat' with identifier '" + id + "'", UserEncashStat.findUserEncashStat(id));
    }
    
}
