// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountLog108DataOnDemand;
import com.palmcommerce.server.v29.db.UserAccountLog108IntegrationTest;
import com.palmcommerce.server.v29.service.UserAccountLogService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAccountLog108IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAccountLog108IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAccountLog108IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAccountLog108IntegrationTest: @Transactional;
    
    @Autowired
    UserAccountLog108DataOnDemand UserAccountLog108IntegrationTest.dod;
    
    @Autowired
    UserAccountLogService UserAccountLog108IntegrationTest.userAccountLogService;
    
    @Test
    public void UserAccountLog108IntegrationTest.testCountAllUserAccountLog108s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to initialize correctly", dod.getRandomUserAccountLog108());
        long count = userAccountLogService.countAllUserAccountLog108s();
        Assert.assertTrue("Counter for 'UserAccountLog108' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAccountLog108IntegrationTest.testFindUserAccountLog108() {
        UserAccountLog108 obj = dod.getRandomUserAccountLog108();
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog108(id);
        Assert.assertNotNull("Find method for 'UserAccountLog108' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAccountLog108' returned the incorrect identifier", id, obj.getLogId());
    }
    
    @Test
    public void UserAccountLog108IntegrationTest.testFindAllUserAccountLog108s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to initialize correctly", dod.getRandomUserAccountLog108());
        long count = userAccountLogService.countAllUserAccountLog108s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAccountLog108', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAccountLog108> result = userAccountLogService.findAllUserAccountLog108s();
        Assert.assertNotNull("Find all method for 'UserAccountLog108' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAccountLog108' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAccountLog108IntegrationTest.testFindUserAccountLog108Entries() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to initialize correctly", dod.getRandomUserAccountLog108());
        long count = userAccountLogService.countAllUserAccountLog108s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAccountLog108> result = userAccountLogService.findUserAccountLog108Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAccountLog108' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAccountLog108' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAccountLog108IntegrationTest.testSaveUserAccountLog108() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to initialize correctly", dod.getRandomUserAccountLog108());
        UserAccountLog108 obj = dod.getNewTransientUserAccountLog108(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserAccountLog108' identifier to be null", obj.getLogId());
        userAccountLogService.saveUserAccountLog108(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserAccountLog108' identifier to no longer be null", obj.getLogId());
    }
    
    @Test
    public void UserAccountLog108IntegrationTest.testDeleteUserAccountLog108() {
        UserAccountLog108 obj = dod.getRandomUserAccountLog108();
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog108' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog108(id);
        userAccountLogService.deleteUserAccountLog108(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAccountLog108' with identifier '" + id + "'", userAccountLogService.findUserAccountLog108(id));
    }
    
}
