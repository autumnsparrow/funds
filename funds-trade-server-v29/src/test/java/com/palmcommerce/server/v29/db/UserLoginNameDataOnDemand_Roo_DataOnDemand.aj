// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserLoginName;
import com.palmcommerce.server.v29.db.UserLoginNameDataOnDemand;
import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect UserLoginNameDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserLoginNameDataOnDemand: @Component;
    
    private Random UserLoginNameDataOnDemand.rnd = new SecureRandom();
    
    private List<UserLoginName> UserLoginNameDataOnDemand.data;
    
    public UserLoginName UserLoginNameDataOnDemand.getNewTransientUserLoginName(int index) {
        UserLoginName obj = new UserLoginName();
        setCreateTime(obj, index);
        setStatus(obj, index);
        setUserId(obj, index);
        return obj;
    }
    
    public void UserLoginNameDataOnDemand.setCreateTime(UserLoginName obj, int index) {
        Date createTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreateTime(createTime);
    }
    
    public void UserLoginNameDataOnDemand.setStatus(UserLoginName obj, int index) {
        BigDecimal status = BigDecimal.valueOf(index);
        obj.setStatus(status);
    }
    
    public void UserLoginNameDataOnDemand.setUserId(UserLoginName obj, int index) {
        String userId = "userId_" + index;
        if (userId.length() > 30) {
            userId = userId.substring(0, 30);
        }
        obj.setUserId(userId);
    }
    
    public UserLoginName UserLoginNameDataOnDemand.getSpecificUserLoginName(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserLoginName obj = data.get(index);
        String id = obj.getLoginName();
        return UserLoginName.findUserLoginName(id);
    }
    
    public UserLoginName UserLoginNameDataOnDemand.getRandomUserLoginName() {
        init();
        UserLoginName obj = data.get(rnd.nextInt(data.size()));
        String id = obj.getLoginName();
        return UserLoginName.findUserLoginName(id);
    }
    
    public boolean UserLoginNameDataOnDemand.modifyUserLoginName(UserLoginName obj) {
        return false;
    }
    
    public void UserLoginNameDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UserLoginName.findUserLoginNameEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserLoginName' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserLoginName>();
        for (int i = 0; i < 10; i++) {
            UserLoginName obj = getNewTransientUserLoginName(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
