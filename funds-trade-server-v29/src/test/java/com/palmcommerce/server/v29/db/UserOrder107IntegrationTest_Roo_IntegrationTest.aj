// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserOrder107;
import com.palmcommerce.server.v29.db.UserOrder107DataOnDemand;
import com.palmcommerce.server.v29.db.UserOrder107IntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserOrder107IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserOrder107IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserOrder107IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserOrder107IntegrationTest: @Transactional;
    
    @Autowired
    UserOrder107DataOnDemand UserOrder107IntegrationTest.dod;
    
    @Test
    public void UserOrder107IntegrationTest.testCountUserOrder107s() {
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to initialize correctly", dod.getRandomUserOrder107());
        long count = UserOrder107.countUserOrder107s();
        Assert.assertTrue("Counter for 'UserOrder107' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserOrder107IntegrationTest.testFindUserOrder107() {
        UserOrder107 obj = dod.getRandomUserOrder107();
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to provide an identifier", id);
        obj = UserOrder107.findUserOrder107(id);
        Assert.assertNotNull("Find method for 'UserOrder107' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserOrder107' returned the incorrect identifier", id, obj.getOrderId());
    }
    
    @Test
    public void UserOrder107IntegrationTest.testFindAllUserOrder107s() {
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to initialize correctly", dod.getRandomUserOrder107());
        long count = UserOrder107.countUserOrder107s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserOrder107', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserOrder107> result = UserOrder107.findAllUserOrder107s();
        Assert.assertNotNull("Find all method for 'UserOrder107' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserOrder107' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserOrder107IntegrationTest.testFindUserOrder107Entries() {
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to initialize correctly", dod.getRandomUserOrder107());
        long count = UserOrder107.countUserOrder107s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserOrder107> result = UserOrder107.findUserOrder107Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserOrder107' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserOrder107' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserOrder107IntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to initialize correctly", dod.getRandomUserOrder107());
        UserOrder107 obj = dod.getNewTransientUserOrder107(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserOrder107' identifier to be null", obj.getOrderId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserOrder107' identifier to no longer be null", obj.getOrderId());
    }
    
    @Test
    public void UserOrder107IntegrationTest.testRemove() {
        UserOrder107 obj = dod.getRandomUserOrder107();
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder107' failed to provide an identifier", id);
        obj = UserOrder107.findUserOrder107(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserOrder107' with identifier '" + id + "'", UserOrder107.findUserOrder107(id));
    }
    
}
