// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserOrder109;
import com.palmcommerce.server.v29.db.UserOrder109DataOnDemand;
import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect UserOrder109DataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserOrder109DataOnDemand: @Component;
    
    private Random UserOrder109DataOnDemand.rnd = new SecureRandom();
    
    private List<UserOrder109> UserOrder109DataOnDemand.data;
    
    public UserOrder109 UserOrder109DataOnDemand.getNewTransientUserOrder109(int index) {
        UserOrder109 obj = new UserOrder109();
        setCreateTime(obj, index);
        setFailMoney(obj, index);
        setFailNum(obj, index);
        setGameId(obj, index);
        setIssueNo(obj, index);
        setOrderStatus(obj, index);
        setPartnerId(obj, index);
        setPayType(obj, index);
        setPorderId(obj, index);
        setPrizeMoney(obj, index);
        setPrizeMoneyAfterTax(obj, index);
        setPrizeStatus(obj, index);
        setPrizeType(obj, index);
        setSucMoney(obj, index);
        setSucNum(obj, index);
        setTicketNum(obj, index);
        setTotalMoney(obj, index);
        setUserId(obj, index);
        return obj;
    }
    
    public void UserOrder109DataOnDemand.setCreateTime(UserOrder109 obj, int index) {
        Date createTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreateTime(createTime);
    }
    
    public void UserOrder109DataOnDemand.setFailMoney(UserOrder109 obj, int index) {
        BigDecimal failMoney = BigDecimal.valueOf(index);
        if (failMoney.compareTo(new BigDecimal("9999999999999.99")) == 1) {
            failMoney = new BigDecimal("9999999999999.99");
        }
        obj.setFailMoney(failMoney);
    }
    
    public void UserOrder109DataOnDemand.setFailNum(UserOrder109 obj, int index) {
        BigDecimal failNum = BigDecimal.valueOf(index);
        obj.setFailNum(failNum);
    }
    
    public void UserOrder109DataOnDemand.setGameId(UserOrder109 obj, int index) {
        String gameId = "gameId_" + index;
        if (gameId.length() > 10) {
            gameId = gameId.substring(0, 10);
        }
        obj.setGameId(gameId);
    }
    
    public void UserOrder109DataOnDemand.setIssueNo(UserOrder109 obj, int index) {
        String issueNo = "issueNo_" + index;
        if (issueNo.length() > 20) {
            issueNo = issueNo.substring(0, 20);
        }
        obj.setIssueNo(issueNo);
    }
    
    public void UserOrder109DataOnDemand.setOrderStatus(UserOrder109 obj, int index) {
        BigDecimal orderStatus = BigDecimal.valueOf(index);
        obj.setOrderStatus(orderStatus);
    }
    
    public void UserOrder109DataOnDemand.setPartnerId(UserOrder109 obj, int index) {
        String partnerId = "partnerId_" + index;
        if (partnerId.length() > 30) {
            partnerId = new Random().nextInt(10) + partnerId.substring(1, 30);
        }
        obj.setPartnerId(partnerId);
    }
    
    public void UserOrder109DataOnDemand.setPayType(UserOrder109 obj, int index) {
        BigDecimal payType = BigDecimal.valueOf(index);
        obj.setPayType(payType);
    }
    
    public void UserOrder109DataOnDemand.setPorderId(UserOrder109 obj, int index) {
        String porderId = "porderId_" + index;
        if (porderId.length() > 30) {
            porderId = new Random().nextInt(10) + porderId.substring(1, 30);
        }
        obj.setPorderId(porderId);
    }
    
    public void UserOrder109DataOnDemand.setPrizeMoney(UserOrder109 obj, int index) {
        BigDecimal prizeMoney = BigDecimal.valueOf(index);
        if (prizeMoney.compareTo(new BigDecimal("9999999999999.99")) == 1) {
            prizeMoney = new BigDecimal("9999999999999.99");
        }
        obj.setPrizeMoney(prizeMoney);
    }
    
    public void UserOrder109DataOnDemand.setPrizeMoneyAfterTax(UserOrder109 obj, int index) {
        BigDecimal prizeMoneyAfterTax = BigDecimal.valueOf(index);
        if (prizeMoneyAfterTax.compareTo(new BigDecimal("9999999999999.99")) == 1) {
            prizeMoneyAfterTax = new BigDecimal("9999999999999.99");
        }
        obj.setPrizeMoneyAfterTax(prizeMoneyAfterTax);
    }
    
    public void UserOrder109DataOnDemand.setPrizeStatus(UserOrder109 obj, int index) {
        BigDecimal prizeStatus = BigDecimal.valueOf(index);
        obj.setPrizeStatus(prizeStatus);
    }
    
    public void UserOrder109DataOnDemand.setPrizeType(UserOrder109 obj, int index) {
        BigDecimal prizeType = BigDecimal.valueOf(index);
        obj.setPrizeType(prizeType);
    }
    
    public void UserOrder109DataOnDemand.setSucMoney(UserOrder109 obj, int index) {
        BigDecimal sucMoney = BigDecimal.valueOf(index);
        if (sucMoney.compareTo(new BigDecimal("9999999999999.99")) == 1) {
            sucMoney = new BigDecimal("9999999999999.99");
        }
        obj.setSucMoney(sucMoney);
    }
    
    public void UserOrder109DataOnDemand.setSucNum(UserOrder109 obj, int index) {
        BigDecimal sucNum = BigDecimal.valueOf(index);
        obj.setSucNum(sucNum);
    }
    
    public void UserOrder109DataOnDemand.setTicketNum(UserOrder109 obj, int index) {
        BigDecimal ticketNum = BigDecimal.valueOf(index);
        obj.setTicketNum(ticketNum);
    }
    
    public void UserOrder109DataOnDemand.setTotalMoney(UserOrder109 obj, int index) {
        BigDecimal totalMoney = BigDecimal.valueOf(index);
        if (totalMoney.compareTo(new BigDecimal("9999999999999.99")) == 1) {
            totalMoney = new BigDecimal("9999999999999.99");
        }
        obj.setTotalMoney(totalMoney);
    }
    
    public void UserOrder109DataOnDemand.setUserId(UserOrder109 obj, int index) {
        String userId = "userId_" + index;
        if (userId.length() > 30) {
            userId = userId.substring(0, 30);
        }
        obj.setUserId(userId);
    }
    
    public UserOrder109 UserOrder109DataOnDemand.getSpecificUserOrder109(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserOrder109 obj = data.get(index);
        String id = obj.getOrderId();
        return UserOrder109.findUserOrder109(id);
    }
    
    public UserOrder109 UserOrder109DataOnDemand.getRandomUserOrder109() {
        init();
        UserOrder109 obj = data.get(rnd.nextInt(data.size()));
        String id = obj.getOrderId();
        return UserOrder109.findUserOrder109(id);
    }
    
    public boolean UserOrder109DataOnDemand.modifyUserOrder109(UserOrder109 obj) {
        return false;
    }
    
    public void UserOrder109DataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UserOrder109.findUserOrder109Entries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserOrder109' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserOrder109>();
        for (int i = 0; i < 10; i++) {
            UserOrder109 obj = getNewTransientUserOrder109(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
