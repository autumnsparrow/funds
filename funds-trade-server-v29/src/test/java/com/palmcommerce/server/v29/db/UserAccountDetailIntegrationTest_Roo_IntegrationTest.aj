// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountDetail;
import com.palmcommerce.server.v29.db.UserAccountDetailDataOnDemand;
import com.palmcommerce.server.v29.db.UserAccountDetailIntegrationTest;
import com.palmcommerce.server.v29.db.UserAccountDetailPK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAccountDetailIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAccountDetailIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAccountDetailIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAccountDetailIntegrationTest: @Transactional;
    
    @Autowired
    UserAccountDetailDataOnDemand UserAccountDetailIntegrationTest.dod;
    
    @Test
    public void UserAccountDetailIntegrationTest.testCountUserAccountDetails() {
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to initialize correctly", dod.getRandomUserAccountDetail());
        long count = UserAccountDetail.countUserAccountDetails();
        Assert.assertTrue("Counter for 'UserAccountDetail' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAccountDetailIntegrationTest.testFindUserAccountDetail() {
        UserAccountDetail obj = dod.getRandomUserAccountDetail();
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to initialize correctly", obj);
        UserAccountDetailPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to provide an identifier", id);
        obj = UserAccountDetail.findUserAccountDetail(id);
        Assert.assertNotNull("Find method for 'UserAccountDetail' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAccountDetail' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UserAccountDetailIntegrationTest.testFindAllUserAccountDetails() {
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to initialize correctly", dod.getRandomUserAccountDetail());
        long count = UserAccountDetail.countUserAccountDetails();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAccountDetail', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAccountDetail> result = UserAccountDetail.findAllUserAccountDetails();
        Assert.assertNotNull("Find all method for 'UserAccountDetail' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAccountDetail' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAccountDetailIntegrationTest.testFindUserAccountDetailEntries() {
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to initialize correctly", dod.getRandomUserAccountDetail());
        long count = UserAccountDetail.countUserAccountDetails();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAccountDetail> result = UserAccountDetail.findUserAccountDetailEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAccountDetail' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAccountDetail' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAccountDetailIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to initialize correctly", dod.getRandomUserAccountDetail());
        UserAccountDetail obj = dod.getNewTransientUserAccountDetail(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserAccountDetail' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UserAccountDetailIntegrationTest.testRemove() {
        UserAccountDetail obj = dod.getRandomUserAccountDetail();
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to initialize correctly", obj);
        UserAccountDetailPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserAccountDetail' failed to provide an identifier", id);
        obj = UserAccountDetail.findUserAccountDetail(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAccountDetail' with identifier '" + id + "'", UserAccountDetail.findUserAccountDetail(id));
    }
    
}
