// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserGiftCoupon;
import com.palmcommerce.server.v29.db.UserGiftCouponDataOnDemand;
import com.palmcommerce.server.v29.db.UserGiftCouponIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserGiftCouponIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserGiftCouponIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserGiftCouponIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserGiftCouponIntegrationTest: @Transactional;
    
    @Autowired
    UserGiftCouponDataOnDemand UserGiftCouponIntegrationTest.dod;
    
    @Test
    public void UserGiftCouponIntegrationTest.testCountUserGiftCoupons() {
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to initialize correctly", dod.getRandomUserGiftCoupon());
        long count = UserGiftCoupon.countUserGiftCoupons();
        Assert.assertTrue("Counter for 'UserGiftCoupon' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserGiftCouponIntegrationTest.testFindUserGiftCoupon() {
        UserGiftCoupon obj = dod.getRandomUserGiftCoupon();
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to initialize correctly", obj);
        String id = obj.getCouponId();
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to provide an identifier", id);
        obj = UserGiftCoupon.findUserGiftCoupon(id);
        Assert.assertNotNull("Find method for 'UserGiftCoupon' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserGiftCoupon' returned the incorrect identifier", id, obj.getCouponId());
    }
    
    @Test
    public void UserGiftCouponIntegrationTest.testFindAllUserGiftCoupons() {
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to initialize correctly", dod.getRandomUserGiftCoupon());
        long count = UserGiftCoupon.countUserGiftCoupons();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserGiftCoupon', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserGiftCoupon> result = UserGiftCoupon.findAllUserGiftCoupons();
        Assert.assertNotNull("Find all method for 'UserGiftCoupon' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserGiftCoupon' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserGiftCouponIntegrationTest.testFindUserGiftCouponEntries() {
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to initialize correctly", dod.getRandomUserGiftCoupon());
        long count = UserGiftCoupon.countUserGiftCoupons();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserGiftCoupon> result = UserGiftCoupon.findUserGiftCouponEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserGiftCoupon' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserGiftCoupon' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserGiftCouponIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to initialize correctly", dod.getRandomUserGiftCoupon());
        UserGiftCoupon obj = dod.getNewTransientUserGiftCoupon(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserGiftCoupon' identifier to be null", obj.getCouponId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserGiftCoupon' identifier to no longer be null", obj.getCouponId());
    }
    
    @Test
    public void UserGiftCouponIntegrationTest.testRemove() {
        UserGiftCoupon obj = dod.getRandomUserGiftCoupon();
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to initialize correctly", obj);
        String id = obj.getCouponId();
        Assert.assertNotNull("Data on demand for 'UserGiftCoupon' failed to provide an identifier", id);
        obj = UserGiftCoupon.findUserGiftCoupon(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserGiftCoupon' with identifier '" + id + "'", UserGiftCoupon.findUserGiftCoupon(id));
    }
    
}
