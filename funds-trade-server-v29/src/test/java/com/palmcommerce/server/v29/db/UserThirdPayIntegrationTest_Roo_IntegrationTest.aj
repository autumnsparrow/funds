// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserThirdPay;
import com.palmcommerce.server.v29.db.UserThirdPayDataOnDemand;
import com.palmcommerce.server.v29.db.UserThirdPayIntegrationTest;
import com.palmcommerce.server.v29.db.UserThirdPayPK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserThirdPayIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserThirdPayIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserThirdPayIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserThirdPayIntegrationTest: @Transactional;
    
    @Autowired
    UserThirdPayDataOnDemand UserThirdPayIntegrationTest.dod;
    
    @Test
    public void UserThirdPayIntegrationTest.testCountUserThirdPays() {
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to initialize correctly", dod.getRandomUserThirdPay());
        long count = UserThirdPay.countUserThirdPays();
        Assert.assertTrue("Counter for 'UserThirdPay' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserThirdPayIntegrationTest.testFindUserThirdPay() {
        UserThirdPay obj = dod.getRandomUserThirdPay();
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to initialize correctly", obj);
        UserThirdPayPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to provide an identifier", id);
        obj = UserThirdPay.findUserThirdPay(id);
        Assert.assertNotNull("Find method for 'UserThirdPay' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserThirdPay' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UserThirdPayIntegrationTest.testFindAllUserThirdPays() {
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to initialize correctly", dod.getRandomUserThirdPay());
        long count = UserThirdPay.countUserThirdPays();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserThirdPay', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserThirdPay> result = UserThirdPay.findAllUserThirdPays();
        Assert.assertNotNull("Find all method for 'UserThirdPay' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserThirdPay' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserThirdPayIntegrationTest.testFindUserThirdPayEntries() {
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to initialize correctly", dod.getRandomUserThirdPay());
        long count = UserThirdPay.countUserThirdPays();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserThirdPay> result = UserThirdPay.findUserThirdPayEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserThirdPay' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserThirdPay' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserThirdPayIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to initialize correctly", dod.getRandomUserThirdPay());
        UserThirdPay obj = dod.getNewTransientUserThirdPay(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserThirdPay' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UserThirdPayIntegrationTest.testRemove() {
        UserThirdPay obj = dod.getRandomUserThirdPay();
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to initialize correctly", obj);
        UserThirdPayPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserThirdPay' failed to provide an identifier", id);
        obj = UserThirdPay.findUserThirdPay(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserThirdPay' with identifier '" + id + "'", UserThirdPay.findUserThirdPay(id));
    }
    
}
