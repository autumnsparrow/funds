// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserBankAccount;
import com.palmcommerce.server.v29.db.UserBankAccountDataOnDemand;
import com.palmcommerce.server.v29.db.UserBankAccountIntegrationTest;
import com.palmcommerce.server.v29.db.UserBankAccountPK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserBankAccountIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserBankAccountIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserBankAccountIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserBankAccountIntegrationTest: @Transactional;
    
    @Autowired
    UserBankAccountDataOnDemand UserBankAccountIntegrationTest.dod;
    
    @Test
    public void UserBankAccountIntegrationTest.testCountUserBankAccounts() {
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to initialize correctly", dod.getRandomUserBankAccount());
        long count = UserBankAccount.countUserBankAccounts();
        Assert.assertTrue("Counter for 'UserBankAccount' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserBankAccountIntegrationTest.testFindUserBankAccount() {
        UserBankAccount obj = dod.getRandomUserBankAccount();
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to initialize correctly", obj);
        UserBankAccountPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to provide an identifier", id);
        obj = UserBankAccount.findUserBankAccount(id);
        Assert.assertNotNull("Find method for 'UserBankAccount' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserBankAccount' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UserBankAccountIntegrationTest.testFindAllUserBankAccounts() {
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to initialize correctly", dod.getRandomUserBankAccount());
        long count = UserBankAccount.countUserBankAccounts();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserBankAccount', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserBankAccount> result = UserBankAccount.findAllUserBankAccounts();
        Assert.assertNotNull("Find all method for 'UserBankAccount' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserBankAccount' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserBankAccountIntegrationTest.testFindUserBankAccountEntries() {
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to initialize correctly", dod.getRandomUserBankAccount());
        long count = UserBankAccount.countUserBankAccounts();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserBankAccount> result = UserBankAccount.findUserBankAccountEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserBankAccount' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserBankAccount' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserBankAccountIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to initialize correctly", dod.getRandomUserBankAccount());
        UserBankAccount obj = dod.getNewTransientUserBankAccount(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserBankAccount' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UserBankAccountIntegrationTest.testRemove() {
        UserBankAccount obj = dod.getRandomUserBankAccount();
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to initialize correctly", obj);
        UserBankAccountPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserBankAccount' failed to provide an identifier", id);
        obj = UserBankAccount.findUserBankAccount(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserBankAccount' with identifier '" + id + "'", UserBankAccount.findUserBankAccount(id));
    }
    
}
