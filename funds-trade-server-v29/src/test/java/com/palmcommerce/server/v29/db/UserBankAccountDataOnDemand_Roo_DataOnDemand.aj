// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserBankAccount;
import com.palmcommerce.server.v29.db.UserBankAccountDataOnDemand;
import com.palmcommerce.server.v29.db.UserBankAccountPK;
import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect UserBankAccountDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserBankAccountDataOnDemand: @Component;
    
    private Random UserBankAccountDataOnDemand.rnd = new SecureRandom();
    
    private List<UserBankAccount> UserBankAccountDataOnDemand.data;
    
    public UserBankAccount UserBankAccountDataOnDemand.getNewTransientUserBankAccount(int index) {
        UserBankAccount obj = new UserBankAccount();
        setEmbeddedIdClass(obj, index);
        return obj;
    }
    
    public void UserBankAccountDataOnDemand.setEmbeddedIdClass(UserBankAccount obj, int index) {
        String seriesNo = "seriesNo_" + index;
        if (seriesNo.length() > 50) {
            seriesNo = new Random().nextInt(10) + seriesNo.substring(1, 50);
        }
        String userId = "userId_" + index;
        if (userId.length() > 20) {
            userId = new Random().nextInt(10) + userId.substring(1, 20);
        }
        String userName = "userName_" + index;
        if (userName.length() > 50) {
            userName = new Random().nextInt(10) + userName.substring(1, 50);
        }
        BigDecimal payMoney = BigDecimal.valueOf(index);
        if (payMoney.compareTo(new BigDecimal("999999999.99")) == 1) {
            payMoney = new BigDecimal("999999999.99");
        }
        Date createTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        String bankCode = "bankCode_" + index;
        if (bankCode.length() > 10) {
            bankCode = new Random().nextInt(10) + bankCode.substring(1, 10);
        }
        Date accountTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        String partnerId = "partnerId_" + index;
        if (partnerId.length() > 20) {
            partnerId = new Random().nextInt(10) + partnerId.substring(1, 20);
        }
        BigDecimal accountType = BigDecimal.valueOf(index);
        BigDecimal balanceMoney = BigDecimal.valueOf(index);
        if (balanceMoney.compareTo(new BigDecimal("999999999.99")) == 1) {
            balanceMoney = new BigDecimal("999999999.99");
        }
        
        UserBankAccountPK embeddedIdClass = new UserBankAccountPK(seriesNo, userId, userName, payMoney, createTime, bankCode, accountTime, partnerId, accountType, balanceMoney);
        obj.setId(embeddedIdClass);
    }
    
    public UserBankAccount UserBankAccountDataOnDemand.getSpecificUserBankAccount(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserBankAccount obj = data.get(index);
        UserBankAccountPK id = obj.getId();
        return UserBankAccount.findUserBankAccount(id);
    }
    
    public UserBankAccount UserBankAccountDataOnDemand.getRandomUserBankAccount() {
        init();
        UserBankAccount obj = data.get(rnd.nextInt(data.size()));
        UserBankAccountPK id = obj.getId();
        return UserBankAccount.findUserBankAccount(id);
    }
    
    public boolean UserBankAccountDataOnDemand.modifyUserBankAccount(UserBankAccount obj) {
        return false;
    }
    
    public void UserBankAccountDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UserBankAccount.findUserBankAccountEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserBankAccount' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserBankAccount>();
        for (int i = 0; i < 10; i++) {
            UserBankAccount obj = getNewTransientUserBankAccount(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
