// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserInfoCancle;
import com.palmcommerce.server.v29.db.UserInfoCancleDataOnDemand;
import com.palmcommerce.server.v29.db.UserInfoCancleIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserInfoCancleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserInfoCancleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserInfoCancleIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserInfoCancleIntegrationTest: @Transactional;
    
    @Autowired
    UserInfoCancleDataOnDemand UserInfoCancleIntegrationTest.dod;
    
    @Test
    public void UserInfoCancleIntegrationTest.testCountUserInfoCancles() {
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to initialize correctly", dod.getRandomUserInfoCancle());
        long count = UserInfoCancle.countUserInfoCancles();
        Assert.assertTrue("Counter for 'UserInfoCancle' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserInfoCancleIntegrationTest.testFindUserInfoCancle() {
        UserInfoCancle obj = dod.getRandomUserInfoCancle();
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to initialize correctly", obj);
        String id = obj.getUserId();
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to provide an identifier", id);
        obj = UserInfoCancle.findUserInfoCancle(id);
        Assert.assertNotNull("Find method for 'UserInfoCancle' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserInfoCancle' returned the incorrect identifier", id, obj.getUserId());
    }
    
    @Test
    public void UserInfoCancleIntegrationTest.testFindAllUserInfoCancles() {
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to initialize correctly", dod.getRandomUserInfoCancle());
        long count = UserInfoCancle.countUserInfoCancles();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserInfoCancle', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserInfoCancle> result = UserInfoCancle.findAllUserInfoCancles();
        Assert.assertNotNull("Find all method for 'UserInfoCancle' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserInfoCancle' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserInfoCancleIntegrationTest.testFindUserInfoCancleEntries() {
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to initialize correctly", dod.getRandomUserInfoCancle());
        long count = UserInfoCancle.countUserInfoCancles();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserInfoCancle> result = UserInfoCancle.findUserInfoCancleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserInfoCancle' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserInfoCancle' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserInfoCancleIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to initialize correctly", dod.getRandomUserInfoCancle());
        UserInfoCancle obj = dod.getNewTransientUserInfoCancle(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserInfoCancle' identifier to be null", obj.getUserId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserInfoCancle' identifier to no longer be null", obj.getUserId());
    }
    
    @Test
    public void UserInfoCancleIntegrationTest.testRemove() {
        UserInfoCancle obj = dod.getRandomUserInfoCancle();
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to initialize correctly", obj);
        String id = obj.getUserId();
        Assert.assertNotNull("Data on demand for 'UserInfoCancle' failed to provide an identifier", id);
        obj = UserInfoCancle.findUserInfoCancle(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserInfoCancle' with identifier '" + id + "'", UserInfoCancle.findUserInfoCancle(id));
    }
    
}
