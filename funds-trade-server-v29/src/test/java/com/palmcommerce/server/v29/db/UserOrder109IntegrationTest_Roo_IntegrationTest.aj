// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserOrder109;
import com.palmcommerce.server.v29.db.UserOrder109DataOnDemand;
import com.palmcommerce.server.v29.db.UserOrder109IntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserOrder109IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserOrder109IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserOrder109IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserOrder109IntegrationTest: @Transactional;
    
    @Autowired
    UserOrder109DataOnDemand UserOrder109IntegrationTest.dod;
    
    @Test
    public void UserOrder109IntegrationTest.testCountUserOrder109s() {
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to initialize correctly", dod.getRandomUserOrder109());
        long count = UserOrder109.countUserOrder109s();
        Assert.assertTrue("Counter for 'UserOrder109' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserOrder109IntegrationTest.testFindUserOrder109() {
        UserOrder109 obj = dod.getRandomUserOrder109();
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to provide an identifier", id);
        obj = UserOrder109.findUserOrder109(id);
        Assert.assertNotNull("Find method for 'UserOrder109' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserOrder109' returned the incorrect identifier", id, obj.getOrderId());
    }
    
    @Test
    public void UserOrder109IntegrationTest.testFindAllUserOrder109s() {
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to initialize correctly", dod.getRandomUserOrder109());
        long count = UserOrder109.countUserOrder109s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserOrder109', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserOrder109> result = UserOrder109.findAllUserOrder109s();
        Assert.assertNotNull("Find all method for 'UserOrder109' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserOrder109' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserOrder109IntegrationTest.testFindUserOrder109Entries() {
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to initialize correctly", dod.getRandomUserOrder109());
        long count = UserOrder109.countUserOrder109s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserOrder109> result = UserOrder109.findUserOrder109Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserOrder109' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserOrder109' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserOrder109IntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to initialize correctly", dod.getRandomUserOrder109());
        UserOrder109 obj = dod.getNewTransientUserOrder109(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserOrder109' identifier to be null", obj.getOrderId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserOrder109' identifier to no longer be null", obj.getOrderId());
    }
    
    @Test
    public void UserOrder109IntegrationTest.testRemove() {
        UserOrder109 obj = dod.getRandomUserOrder109();
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder109' failed to provide an identifier", id);
        obj = UserOrder109.findUserOrder109(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserOrder109' with identifier '" + id + "'", UserOrder109.findUserOrder109(id));
    }
    
}
