// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserIdentifyTemp;
import com.palmcommerce.server.v29.db.UserIdentifyTempDataOnDemand;
import com.palmcommerce.server.v29.db.UserIdentifyTempIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserIdentifyTempIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserIdentifyTempIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserIdentifyTempIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserIdentifyTempIntegrationTest: @Transactional;
    
    @Autowired
    UserIdentifyTempDataOnDemand UserIdentifyTempIntegrationTest.dod;
    
    @Test
    public void UserIdentifyTempIntegrationTest.testCountUserIdentifyTemps() {
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to initialize correctly", dod.getRandomUserIdentifyTemp());
        long count = UserIdentifyTemp.countUserIdentifyTemps();
        Assert.assertTrue("Counter for 'UserIdentifyTemp' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserIdentifyTempIntegrationTest.testFindUserIdentifyTemp() {
        UserIdentifyTemp obj = dod.getRandomUserIdentifyTemp();
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to initialize correctly", obj);
        String id = obj.getUserId();
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to provide an identifier", id);
        obj = UserIdentifyTemp.findUserIdentifyTemp(id);
        Assert.assertNotNull("Find method for 'UserIdentifyTemp' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserIdentifyTemp' returned the incorrect identifier", id, obj.getUserId());
    }
    
    @Test
    public void UserIdentifyTempIntegrationTest.testFindAllUserIdentifyTemps() {
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to initialize correctly", dod.getRandomUserIdentifyTemp());
        long count = UserIdentifyTemp.countUserIdentifyTemps();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserIdentifyTemp', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserIdentifyTemp> result = UserIdentifyTemp.findAllUserIdentifyTemps();
        Assert.assertNotNull("Find all method for 'UserIdentifyTemp' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserIdentifyTemp' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserIdentifyTempIntegrationTest.testFindUserIdentifyTempEntries() {
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to initialize correctly", dod.getRandomUserIdentifyTemp());
        long count = UserIdentifyTemp.countUserIdentifyTemps();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserIdentifyTemp> result = UserIdentifyTemp.findUserIdentifyTempEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserIdentifyTemp' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserIdentifyTemp' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserIdentifyTempIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to initialize correctly", dod.getRandomUserIdentifyTemp());
        UserIdentifyTemp obj = dod.getNewTransientUserIdentifyTemp(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserIdentifyTemp' identifier to be null", obj.getUserId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserIdentifyTemp' identifier to no longer be null", obj.getUserId());
    }
    
    @Test
    public void UserIdentifyTempIntegrationTest.testRemove() {
        UserIdentifyTemp obj = dod.getRandomUserIdentifyTemp();
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to initialize correctly", obj);
        String id = obj.getUserId();
        Assert.assertNotNull("Data on demand for 'UserIdentifyTemp' failed to provide an identifier", id);
        obj = UserIdentifyTemp.findUserIdentifyTemp(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserIdentifyTemp' with identifier '" + id + "'", UserIdentifyTemp.findUserIdentifyTemp(id));
    }
    
}
