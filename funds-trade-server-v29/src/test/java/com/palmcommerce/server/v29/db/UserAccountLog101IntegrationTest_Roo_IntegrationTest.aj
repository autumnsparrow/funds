// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountLog101DataOnDemand;
import com.palmcommerce.server.v29.db.UserAccountLog101IntegrationTest;
import com.palmcommerce.server.v29.service.UserAccountLogService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAccountLog101IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAccountLog101IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAccountLog101IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAccountLog101IntegrationTest: @Transactional;
    
    @Autowired
    UserAccountLog101DataOnDemand UserAccountLog101IntegrationTest.dod;
    
    @Autowired
    UserAccountLogService UserAccountLog101IntegrationTest.userAccountLogService;
    
    @Test
    public void UserAccountLog101IntegrationTest.testCountAllUserAccountLog101s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to initialize correctly", dod.getRandomUserAccountLog101());
        long count = userAccountLogService.countAllUserAccountLog101s();
        Assert.assertTrue("Counter for 'UserAccountLog101' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAccountLog101IntegrationTest.testFindUserAccountLog101() {
        UserAccountLog101 obj = dod.getRandomUserAccountLog101();
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog101(id);
        Assert.assertNotNull("Find method for 'UserAccountLog101' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAccountLog101' returned the incorrect identifier", id, obj.getLogId());
    }
    
    @Test
    public void UserAccountLog101IntegrationTest.testFindAllUserAccountLog101s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to initialize correctly", dod.getRandomUserAccountLog101());
        long count = userAccountLogService.countAllUserAccountLog101s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAccountLog101', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAccountLog101> result = userAccountLogService.findAllUserAccountLog101s();
        Assert.assertNotNull("Find all method for 'UserAccountLog101' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAccountLog101' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAccountLog101IntegrationTest.testFindUserAccountLog101Entries() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to initialize correctly", dod.getRandomUserAccountLog101());
        long count = userAccountLogService.countAllUserAccountLog101s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAccountLog101> result = userAccountLogService.findUserAccountLog101Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAccountLog101' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAccountLog101' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAccountLog101IntegrationTest.testSaveUserAccountLog101() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to initialize correctly", dod.getRandomUserAccountLog101());
        UserAccountLog101 obj = dod.getNewTransientUserAccountLog101(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserAccountLog101' identifier to be null", obj.getLogId());
        userAccountLogService.saveUserAccountLog101(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserAccountLog101' identifier to no longer be null", obj.getLogId());
    }
    
    @Test
    public void UserAccountLog101IntegrationTest.testDeleteUserAccountLog101() {
        UserAccountLog101 obj = dod.getRandomUserAccountLog101();
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog101' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog101(id);
        userAccountLogService.deleteUserAccountLog101(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAccountLog101' with identifier '" + id + "'", userAccountLogService.findUserAccountLog101(id));
    }
    
}
