// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountLog102DataOnDemand;
import com.palmcommerce.server.v29.db.UserAccountLog102IntegrationTest;
import com.palmcommerce.server.v29.service.UserAccountLogService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAccountLog102IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAccountLog102IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAccountLog102IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAccountLog102IntegrationTest: @Transactional;
    
    @Autowired
    UserAccountLog102DataOnDemand UserAccountLog102IntegrationTest.dod;
    
    @Autowired
    UserAccountLogService UserAccountLog102IntegrationTest.userAccountLogService;
    
    @Test
    public void UserAccountLog102IntegrationTest.testCountAllUserAccountLog102s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to initialize correctly", dod.getRandomUserAccountLog102());
        long count = userAccountLogService.countAllUserAccountLog102s();
        Assert.assertTrue("Counter for 'UserAccountLog102' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAccountLog102IntegrationTest.testFindUserAccountLog102() {
        UserAccountLog102 obj = dod.getRandomUserAccountLog102();
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog102(id);
        Assert.assertNotNull("Find method for 'UserAccountLog102' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAccountLog102' returned the incorrect identifier", id, obj.getLogId());
    }
    
    @Test
    public void UserAccountLog102IntegrationTest.testFindAllUserAccountLog102s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to initialize correctly", dod.getRandomUserAccountLog102());
        long count = userAccountLogService.countAllUserAccountLog102s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAccountLog102', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAccountLog102> result = userAccountLogService.findAllUserAccountLog102s();
        Assert.assertNotNull("Find all method for 'UserAccountLog102' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAccountLog102' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAccountLog102IntegrationTest.testFindUserAccountLog102Entries() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to initialize correctly", dod.getRandomUserAccountLog102());
        long count = userAccountLogService.countAllUserAccountLog102s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAccountLog102> result = userAccountLogService.findUserAccountLog102Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAccountLog102' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAccountLog102' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAccountLog102IntegrationTest.testSaveUserAccountLog102() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to initialize correctly", dod.getRandomUserAccountLog102());
        UserAccountLog102 obj = dod.getNewTransientUserAccountLog102(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserAccountLog102' identifier to be null", obj.getLogId());
        userAccountLogService.saveUserAccountLog102(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserAccountLog102' identifier to no longer be null", obj.getLogId());
    }
    
    @Test
    public void UserAccountLog102IntegrationTest.testDeleteUserAccountLog102() {
        UserAccountLog102 obj = dod.getRandomUserAccountLog102();
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog102' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog102(id);
        userAccountLogService.deleteUserAccountLog102(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAccountLog102' with identifier '" + id + "'", userAccountLogService.findUserAccountLog102(id));
    }
    
}
