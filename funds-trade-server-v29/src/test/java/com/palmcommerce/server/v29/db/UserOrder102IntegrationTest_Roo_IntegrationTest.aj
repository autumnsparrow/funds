// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserOrder102;
import com.palmcommerce.server.v29.db.UserOrder102DataOnDemand;
import com.palmcommerce.server.v29.db.UserOrder102IntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserOrder102IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserOrder102IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserOrder102IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserOrder102IntegrationTest: @Transactional;
    
    @Autowired
    UserOrder102DataOnDemand UserOrder102IntegrationTest.dod;
    
    @Test
    public void UserOrder102IntegrationTest.testCountUserOrder102s() {
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to initialize correctly", dod.getRandomUserOrder102());
        long count = UserOrder102.countUserOrder102s();
        Assert.assertTrue("Counter for 'UserOrder102' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserOrder102IntegrationTest.testFindUserOrder102() {
        UserOrder102 obj = dod.getRandomUserOrder102();
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to provide an identifier", id);
        obj = UserOrder102.findUserOrder102(id);
        Assert.assertNotNull("Find method for 'UserOrder102' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserOrder102' returned the incorrect identifier", id, obj.getOrderId());
    }
    
    @Test
    public void UserOrder102IntegrationTest.testFindAllUserOrder102s() {
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to initialize correctly", dod.getRandomUserOrder102());
        long count = UserOrder102.countUserOrder102s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserOrder102', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserOrder102> result = UserOrder102.findAllUserOrder102s();
        Assert.assertNotNull("Find all method for 'UserOrder102' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserOrder102' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserOrder102IntegrationTest.testFindUserOrder102Entries() {
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to initialize correctly", dod.getRandomUserOrder102());
        long count = UserOrder102.countUserOrder102s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserOrder102> result = UserOrder102.findUserOrder102Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserOrder102' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserOrder102' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserOrder102IntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to initialize correctly", dod.getRandomUserOrder102());
        UserOrder102 obj = dod.getNewTransientUserOrder102(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserOrder102' identifier to be null", obj.getOrderId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserOrder102' identifier to no longer be null", obj.getOrderId());
    }
    
    @Test
    public void UserOrder102IntegrationTest.testRemove() {
        UserOrder102 obj = dod.getRandomUserOrder102();
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder102' failed to provide an identifier", id);
        obj = UserOrder102.findUserOrder102(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserOrder102' with identifier '" + id + "'", UserOrder102.findUserOrder102(id));
    }
    
}
