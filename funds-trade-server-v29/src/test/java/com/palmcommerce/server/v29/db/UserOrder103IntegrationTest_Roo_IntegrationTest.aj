// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserOrder103;
import com.palmcommerce.server.v29.db.UserOrder103DataOnDemand;
import com.palmcommerce.server.v29.db.UserOrder103IntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserOrder103IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserOrder103IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserOrder103IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserOrder103IntegrationTest: @Transactional;
    
    @Autowired
    UserOrder103DataOnDemand UserOrder103IntegrationTest.dod;
    
    @Test
    public void UserOrder103IntegrationTest.testCountUserOrder103s() {
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to initialize correctly", dod.getRandomUserOrder103());
        long count = UserOrder103.countUserOrder103s();
        Assert.assertTrue("Counter for 'UserOrder103' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserOrder103IntegrationTest.testFindUserOrder103() {
        UserOrder103 obj = dod.getRandomUserOrder103();
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to provide an identifier", id);
        obj = UserOrder103.findUserOrder103(id);
        Assert.assertNotNull("Find method for 'UserOrder103' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserOrder103' returned the incorrect identifier", id, obj.getOrderId());
    }
    
    @Test
    public void UserOrder103IntegrationTest.testFindAllUserOrder103s() {
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to initialize correctly", dod.getRandomUserOrder103());
        long count = UserOrder103.countUserOrder103s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserOrder103', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserOrder103> result = UserOrder103.findAllUserOrder103s();
        Assert.assertNotNull("Find all method for 'UserOrder103' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserOrder103' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserOrder103IntegrationTest.testFindUserOrder103Entries() {
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to initialize correctly", dod.getRandomUserOrder103());
        long count = UserOrder103.countUserOrder103s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserOrder103> result = UserOrder103.findUserOrder103Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserOrder103' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserOrder103' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserOrder103IntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to initialize correctly", dod.getRandomUserOrder103());
        UserOrder103 obj = dod.getNewTransientUserOrder103(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserOrder103' identifier to be null", obj.getOrderId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserOrder103' identifier to no longer be null", obj.getOrderId());
    }
    
    @Test
    public void UserOrder103IntegrationTest.testRemove() {
        UserOrder103 obj = dod.getRandomUserOrder103();
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to initialize correctly", obj);
        String id = obj.getOrderId();
        Assert.assertNotNull("Data on demand for 'UserOrder103' failed to provide an identifier", id);
        obj = UserOrder103.findUserOrder103(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserOrder103' with identifier '" + id + "'", UserOrder103.findUserOrder103(id));
    }
    
}
