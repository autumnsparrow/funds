// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserChargeStat;
import com.palmcommerce.server.v29.db.UserChargeStatDataOnDemand;
import com.palmcommerce.server.v29.db.UserChargeStatIntegrationTest;
import java.math.BigDecimal;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserChargeStatIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserChargeStatIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserChargeStatIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserChargeStatIntegrationTest: @Transactional;
    
    @Autowired
    UserChargeStatDataOnDemand UserChargeStatIntegrationTest.dod;
    
    @Test
    public void UserChargeStatIntegrationTest.testCountUserChargeStats() {
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to initialize correctly", dod.getRandomUserChargeStat());
        long count = UserChargeStat.countUserChargeStats();
        Assert.assertTrue("Counter for 'UserChargeStat' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserChargeStatIntegrationTest.testFindUserChargeStat() {
        UserChargeStat obj = dod.getRandomUserChargeStat();
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to initialize correctly", obj);
        BigDecimal id = obj.getStatId();
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to provide an identifier", id);
        obj = UserChargeStat.findUserChargeStat(id);
        Assert.assertNotNull("Find method for 'UserChargeStat' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserChargeStat' returned the incorrect identifier", id, obj.getStatId());
    }
    
    @Test
    public void UserChargeStatIntegrationTest.testFindAllUserChargeStats() {
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to initialize correctly", dod.getRandomUserChargeStat());
        long count = UserChargeStat.countUserChargeStats();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserChargeStat', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserChargeStat> result = UserChargeStat.findAllUserChargeStats();
        Assert.assertNotNull("Find all method for 'UserChargeStat' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserChargeStat' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserChargeStatIntegrationTest.testFindUserChargeStatEntries() {
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to initialize correctly", dod.getRandomUserChargeStat());
        long count = UserChargeStat.countUserChargeStats();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserChargeStat> result = UserChargeStat.findUserChargeStatEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserChargeStat' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserChargeStat' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserChargeStatIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to initialize correctly", dod.getRandomUserChargeStat());
        UserChargeStat obj = dod.getNewTransientUserChargeStat(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserChargeStat' identifier to be null", obj.getStatId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserChargeStat' identifier to no longer be null", obj.getStatId());
    }
    
    @Test
    public void UserChargeStatIntegrationTest.testRemove() {
        UserChargeStat obj = dod.getRandomUserChargeStat();
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to initialize correctly", obj);
        BigDecimal id = obj.getStatId();
        Assert.assertNotNull("Data on demand for 'UserChargeStat' failed to provide an identifier", id);
        obj = UserChargeStat.findUserChargeStat(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserChargeStat' with identifier '" + id + "'", UserChargeStat.findUserChargeStat(id));
    }
    
}
