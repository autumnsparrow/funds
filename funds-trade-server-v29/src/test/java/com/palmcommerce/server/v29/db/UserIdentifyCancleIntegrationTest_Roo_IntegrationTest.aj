// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserIdentifyCancle;
import com.palmcommerce.server.v29.db.UserIdentifyCancleDataOnDemand;
import com.palmcommerce.server.v29.db.UserIdentifyCancleIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserIdentifyCancleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserIdentifyCancleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserIdentifyCancleIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserIdentifyCancleIntegrationTest: @Transactional;
    
    @Autowired
    UserIdentifyCancleDataOnDemand UserIdentifyCancleIntegrationTest.dod;
    
    @Test
    public void UserIdentifyCancleIntegrationTest.testCountUserIdentifyCancles() {
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to initialize correctly", dod.getRandomUserIdentifyCancle());
        long count = UserIdentifyCancle.countUserIdentifyCancles();
        Assert.assertTrue("Counter for 'UserIdentifyCancle' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserIdentifyCancleIntegrationTest.testFindUserIdentifyCancle() {
        UserIdentifyCancle obj = dod.getRandomUserIdentifyCancle();
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to initialize correctly", obj);
        String id = obj.getUserId();
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to provide an identifier", id);
        obj = UserIdentifyCancle.findUserIdentifyCancle(id);
        Assert.assertNotNull("Find method for 'UserIdentifyCancle' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserIdentifyCancle' returned the incorrect identifier", id, obj.getUserId());
    }
    
    @Test
    public void UserIdentifyCancleIntegrationTest.testFindAllUserIdentifyCancles() {
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to initialize correctly", dod.getRandomUserIdentifyCancle());
        long count = UserIdentifyCancle.countUserIdentifyCancles();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserIdentifyCancle', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserIdentifyCancle> result = UserIdentifyCancle.findAllUserIdentifyCancles();
        Assert.assertNotNull("Find all method for 'UserIdentifyCancle' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserIdentifyCancle' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserIdentifyCancleIntegrationTest.testFindUserIdentifyCancleEntries() {
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to initialize correctly", dod.getRandomUserIdentifyCancle());
        long count = UserIdentifyCancle.countUserIdentifyCancles();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserIdentifyCancle> result = UserIdentifyCancle.findUserIdentifyCancleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserIdentifyCancle' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserIdentifyCancle' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserIdentifyCancleIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to initialize correctly", dod.getRandomUserIdentifyCancle());
        UserIdentifyCancle obj = dod.getNewTransientUserIdentifyCancle(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserIdentifyCancle' identifier to be null", obj.getUserId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserIdentifyCancle' identifier to no longer be null", obj.getUserId());
    }
    
    @Test
    public void UserIdentifyCancleIntegrationTest.testRemove() {
        UserIdentifyCancle obj = dod.getRandomUserIdentifyCancle();
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to initialize correctly", obj);
        String id = obj.getUserId();
        Assert.assertNotNull("Data on demand for 'UserIdentifyCancle' failed to provide an identifier", id);
        obj = UserIdentifyCancle.findUserIdentifyCancle(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserIdentifyCancle' with identifier '" + id + "'", UserIdentifyCancle.findUserIdentifyCancle(id));
    }
    
}
