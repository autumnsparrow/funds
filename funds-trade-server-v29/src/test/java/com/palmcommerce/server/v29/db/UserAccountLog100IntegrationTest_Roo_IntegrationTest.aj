// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountLog100DataOnDemand;
import com.palmcommerce.server.v29.db.UserAccountLog100IntegrationTest;
import com.palmcommerce.server.v29.service.UserAccountLogService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAccountLog100IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAccountLog100IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAccountLog100IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAccountLog100IntegrationTest: @Transactional;
    
    @Autowired
    UserAccountLog100DataOnDemand UserAccountLog100IntegrationTest.dod;
    
    @Autowired
    UserAccountLogService UserAccountLog100IntegrationTest.userAccountLogService;
    
    @Test
    public void UserAccountLog100IntegrationTest.testCountAllUserAccountLog100s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to initialize correctly", dod.getRandomUserAccountLog100());
        long count = userAccountLogService.countAllUserAccountLog100s();
        Assert.assertTrue("Counter for 'UserAccountLog100' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAccountLog100IntegrationTest.testFindUserAccountLog100() {
        UserAccountLog100 obj = dod.getRandomUserAccountLog100();
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog100(id);
        Assert.assertNotNull("Find method for 'UserAccountLog100' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAccountLog100' returned the incorrect identifier", id, obj.getLogId());
    }
    
    @Test
    public void UserAccountLog100IntegrationTest.testFindAllUserAccountLog100s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to initialize correctly", dod.getRandomUserAccountLog100());
        long count = userAccountLogService.countAllUserAccountLog100s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAccountLog100', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAccountLog100> result = userAccountLogService.findAllUserAccountLog100s();
        Assert.assertNotNull("Find all method for 'UserAccountLog100' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAccountLog100' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAccountLog100IntegrationTest.testFindUserAccountLog100Entries() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to initialize correctly", dod.getRandomUserAccountLog100());
        long count = userAccountLogService.countAllUserAccountLog100s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAccountLog100> result = userAccountLogService.findUserAccountLog100Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAccountLog100' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAccountLog100' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAccountLog100IntegrationTest.testSaveUserAccountLog100() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to initialize correctly", dod.getRandomUserAccountLog100());
        UserAccountLog100 obj = dod.getNewTransientUserAccountLog100(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserAccountLog100' identifier to be null", obj.getLogId());
        userAccountLogService.saveUserAccountLog100(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserAccountLog100' identifier to no longer be null", obj.getLogId());
    }
    
    @Test
    public void UserAccountLog100IntegrationTest.testDeleteUserAccountLog100() {
        UserAccountLog100 obj = dod.getRandomUserAccountLog100();
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog100' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog100(id);
        userAccountLogService.deleteUserAccountLog100(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAccountLog100' with identifier '" + id + "'", userAccountLogService.findUserAccountLog100(id));
    }
    
}
