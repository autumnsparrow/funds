// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserStakeAccount;
import com.palmcommerce.server.v29.db.UserStakeAccountDataOnDemand;
import com.palmcommerce.server.v29.db.UserStakeAccountIntegrationTest;
import com.palmcommerce.server.v29.db.UserStakeAccountPK;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserStakeAccountIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserStakeAccountIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserStakeAccountIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserStakeAccountIntegrationTest: @Transactional;
    
    @Autowired
    UserStakeAccountDataOnDemand UserStakeAccountIntegrationTest.dod;
    
    @Test
    public void UserStakeAccountIntegrationTest.testCountUserStakeAccounts() {
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to initialize correctly", dod.getRandomUserStakeAccount());
        long count = UserStakeAccount.countUserStakeAccounts();
        Assert.assertTrue("Counter for 'UserStakeAccount' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserStakeAccountIntegrationTest.testFindUserStakeAccount() {
        UserStakeAccount obj = dod.getRandomUserStakeAccount();
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to initialize correctly", obj);
        UserStakeAccountPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to provide an identifier", id);
        obj = UserStakeAccount.findUserStakeAccount(id);
        Assert.assertNotNull("Find method for 'UserStakeAccount' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserStakeAccount' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UserStakeAccountIntegrationTest.testFindAllUserStakeAccounts() {
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to initialize correctly", dod.getRandomUserStakeAccount());
        long count = UserStakeAccount.countUserStakeAccounts();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserStakeAccount', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserStakeAccount> result = UserStakeAccount.findAllUserStakeAccounts();
        Assert.assertNotNull("Find all method for 'UserStakeAccount' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserStakeAccount' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserStakeAccountIntegrationTest.testFindUserStakeAccountEntries() {
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to initialize correctly", dod.getRandomUserStakeAccount());
        long count = UserStakeAccount.countUserStakeAccounts();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserStakeAccount> result = UserStakeAccount.findUserStakeAccountEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserStakeAccount' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserStakeAccount' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserStakeAccountIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to initialize correctly", dod.getRandomUserStakeAccount());
        UserStakeAccount obj = dod.getNewTransientUserStakeAccount(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserStakeAccount' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UserStakeAccountIntegrationTest.testRemove() {
        UserStakeAccount obj = dod.getRandomUserStakeAccount();
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to initialize correctly", obj);
        UserStakeAccountPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserStakeAccount' failed to provide an identifier", id);
        obj = UserStakeAccount.findUserStakeAccount(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserStakeAccount' with identifier '" + id + "'", UserStakeAccount.findUserStakeAccount(id));
    }
    
}
