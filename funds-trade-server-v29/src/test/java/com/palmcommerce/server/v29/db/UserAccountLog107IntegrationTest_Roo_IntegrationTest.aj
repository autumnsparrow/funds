// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountLog107DataOnDemand;
import com.palmcommerce.server.v29.db.UserAccountLog107IntegrationTest;
import com.palmcommerce.server.v29.service.UserAccountLogService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAccountLog107IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAccountLog107IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAccountLog107IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAccountLog107IntegrationTest: @Transactional;
    
    @Autowired
    UserAccountLog107DataOnDemand UserAccountLog107IntegrationTest.dod;
    
    @Autowired
    UserAccountLogService UserAccountLog107IntegrationTest.userAccountLogService;
    
    @Test
    public void UserAccountLog107IntegrationTest.testCountAllUserAccountLog107s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to initialize correctly", dod.getRandomUserAccountLog107());
        long count = userAccountLogService.countAllUserAccountLog107s();
        Assert.assertTrue("Counter for 'UserAccountLog107' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAccountLog107IntegrationTest.testFindUserAccountLog107() {
        UserAccountLog107 obj = dod.getRandomUserAccountLog107();
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog107(id);
        Assert.assertNotNull("Find method for 'UserAccountLog107' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAccountLog107' returned the incorrect identifier", id, obj.getLogId());
    }
    
    @Test
    public void UserAccountLog107IntegrationTest.testFindAllUserAccountLog107s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to initialize correctly", dod.getRandomUserAccountLog107());
        long count = userAccountLogService.countAllUserAccountLog107s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAccountLog107', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAccountLog107> result = userAccountLogService.findAllUserAccountLog107s();
        Assert.assertNotNull("Find all method for 'UserAccountLog107' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAccountLog107' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAccountLog107IntegrationTest.testFindUserAccountLog107Entries() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to initialize correctly", dod.getRandomUserAccountLog107());
        long count = userAccountLogService.countAllUserAccountLog107s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAccountLog107> result = userAccountLogService.findUserAccountLog107Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAccountLog107' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAccountLog107' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAccountLog107IntegrationTest.testSaveUserAccountLog107() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to initialize correctly", dod.getRandomUserAccountLog107());
        UserAccountLog107 obj = dod.getNewTransientUserAccountLog107(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserAccountLog107' identifier to be null", obj.getLogId());
        userAccountLogService.saveUserAccountLog107(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserAccountLog107' identifier to no longer be null", obj.getLogId());
    }
    
    @Test
    public void UserAccountLog107IntegrationTest.testDeleteUserAccountLog107() {
        UserAccountLog107 obj = dod.getRandomUserAccountLog107();
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog107' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog107(id);
        userAccountLogService.deleteUserAccountLog107(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAccountLog107' with identifier '" + id + "'", userAccountLogService.findUserAccountLog107(id));
    }
    
}
