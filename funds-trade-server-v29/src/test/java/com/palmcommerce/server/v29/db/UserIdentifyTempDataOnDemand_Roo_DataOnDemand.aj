// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserIdentifyTemp;
import com.palmcommerce.server.v29.db.UserIdentifyTempDataOnDemand;
import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect UserIdentifyTempDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserIdentifyTempDataOnDemand: @Component;
    
    private Random UserIdentifyTempDataOnDemand.rnd = new SecureRandom();
    
    private List<UserIdentifyTemp> UserIdentifyTempDataOnDemand.data;
    
    public UserIdentifyTemp UserIdentifyTempDataOnDemand.getNewTransientUserIdentifyTemp(int index) {
        UserIdentifyTemp obj = new UserIdentifyTemp();
        setBankBranches(obj, index);
        setBankCity(obj, index);
        setBankProvince(obj, index);
        setBankcard(obj, index);
        setBankname(obj, index);
        setCardNo(obj, index);
        setCardType(obj, index);
        setCreateTime(obj, index);
        setLevelType(obj, index);
        setRealName(obj, index);
        setStatus(obj, index);
        setUpdateTime(obj, index);
        return obj;
    }
    
    public void UserIdentifyTempDataOnDemand.setBankBranches(UserIdentifyTemp obj, int index) {
        String bankBranches = "bankBranches_" + index;
        if (bankBranches.length() > 50) {
            bankBranches = bankBranches.substring(0, 50);
        }
        obj.setBankBranches(bankBranches);
    }
    
    public void UserIdentifyTempDataOnDemand.setBankCity(UserIdentifyTemp obj, int index) {
        String bankCity = "bankCity_" + index;
        if (bankCity.length() > 50) {
            bankCity = bankCity.substring(0, 50);
        }
        obj.setBankCity(bankCity);
    }
    
    public void UserIdentifyTempDataOnDemand.setBankProvince(UserIdentifyTemp obj, int index) {
        String bankProvince = "bankProvince_" + index;
        if (bankProvince.length() > 50) {
            bankProvince = bankProvince.substring(0, 50);
        }
        obj.setBankProvince(bankProvince);
    }
    
    public void UserIdentifyTempDataOnDemand.setBankcard(UserIdentifyTemp obj, int index) {
        String bankcard = "bankcard_" + index;
        if (bankcard.length() > 30) {
            bankcard = bankcard.substring(0, 30);
        }
        obj.setBankcard(bankcard);
    }
    
    public void UserIdentifyTempDataOnDemand.setBankname(UserIdentifyTemp obj, int index) {
        String bankname = "bankname_" + index;
        if (bankname.length() > 30) {
            bankname = bankname.substring(0, 30);
        }
        obj.setBankname(bankname);
    }
    
    public void UserIdentifyTempDataOnDemand.setCardNo(UserIdentifyTemp obj, int index) {
        String cardNo = "cardNo_" + index;
        if (cardNo.length() > 30) {
            cardNo = cardNo.substring(0, 30);
        }
        obj.setCardNo(cardNo);
    }
    
    public void UserIdentifyTempDataOnDemand.setCardType(UserIdentifyTemp obj, int index) {
        String cardType = "c" + index;
        if (cardType.length() > 2) {
            cardType = cardType.substring(0, 2);
        }
        obj.setCardType(cardType);
    }
    
    public void UserIdentifyTempDataOnDemand.setCreateTime(UserIdentifyTemp obj, int index) {
        Date createTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreateTime(createTime);
    }
    
    public void UserIdentifyTempDataOnDemand.setLevelType(UserIdentifyTemp obj, int index) {
        BigDecimal levelType = BigDecimal.valueOf(index);
        obj.setLevelType(levelType);
    }
    
    public void UserIdentifyTempDataOnDemand.setRealName(UserIdentifyTemp obj, int index) {
        String realName = "realName_" + index;
        if (realName.length() > 20) {
            realName = realName.substring(0, 20);
        }
        obj.setRealName(realName);
    }
    
    public void UserIdentifyTempDataOnDemand.setStatus(UserIdentifyTemp obj, int index) {
        BigDecimal status = BigDecimal.valueOf(index);
        obj.setStatus(status);
    }
    
    public void UserIdentifyTempDataOnDemand.setUpdateTime(UserIdentifyTemp obj, int index) {
        Date updateTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdateTime(updateTime);
    }
    
    public UserIdentifyTemp UserIdentifyTempDataOnDemand.getSpecificUserIdentifyTemp(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserIdentifyTemp obj = data.get(index);
        String id = obj.getUserId();
        return UserIdentifyTemp.findUserIdentifyTemp(id);
    }
    
    public UserIdentifyTemp UserIdentifyTempDataOnDemand.getRandomUserIdentifyTemp() {
        init();
        UserIdentifyTemp obj = data.get(rnd.nextInt(data.size()));
        String id = obj.getUserId();
        return UserIdentifyTemp.findUserIdentifyTemp(id);
    }
    
    public boolean UserIdentifyTempDataOnDemand.modifyUserIdentifyTemp(UserIdentifyTemp obj) {
        return false;
    }
    
    public void UserIdentifyTempDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UserIdentifyTemp.findUserIdentifyTempEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserIdentifyTemp' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserIdentifyTemp>();
        for (int i = 0; i < 10; i++) {
            UserIdentifyTemp obj = getNewTransientUserIdentifyTemp(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
