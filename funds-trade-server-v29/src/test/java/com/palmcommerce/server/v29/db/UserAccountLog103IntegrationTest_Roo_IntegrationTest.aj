// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountLog103DataOnDemand;
import com.palmcommerce.server.v29.db.UserAccountLog103IntegrationTest;
import com.palmcommerce.server.v29.service.UserAccountLogService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserAccountLog103IntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserAccountLog103IntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserAccountLog103IntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserAccountLog103IntegrationTest: @Transactional;
    
    @Autowired
    UserAccountLog103DataOnDemand UserAccountLog103IntegrationTest.dod;
    
    @Autowired
    UserAccountLogService UserAccountLog103IntegrationTest.userAccountLogService;
    
    @Test
    public void UserAccountLog103IntegrationTest.testCountAllUserAccountLog103s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to initialize correctly", dod.getRandomUserAccountLog103());
        long count = userAccountLogService.countAllUserAccountLog103s();
        Assert.assertTrue("Counter for 'UserAccountLog103' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserAccountLog103IntegrationTest.testFindUserAccountLog103() {
        UserAccountLog103 obj = dod.getRandomUserAccountLog103();
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog103(id);
        Assert.assertNotNull("Find method for 'UserAccountLog103' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserAccountLog103' returned the incorrect identifier", id, obj.getLogId());
    }
    
    @Test
    public void UserAccountLog103IntegrationTest.testFindAllUserAccountLog103s() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to initialize correctly", dod.getRandomUserAccountLog103());
        long count = userAccountLogService.countAllUserAccountLog103s();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserAccountLog103', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserAccountLog103> result = userAccountLogService.findAllUserAccountLog103s();
        Assert.assertNotNull("Find all method for 'UserAccountLog103' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserAccountLog103' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserAccountLog103IntegrationTest.testFindUserAccountLog103Entries() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to initialize correctly", dod.getRandomUserAccountLog103());
        long count = userAccountLogService.countAllUserAccountLog103s();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserAccountLog103> result = userAccountLogService.findUserAccountLog103Entries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserAccountLog103' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserAccountLog103' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserAccountLog103IntegrationTest.testSaveUserAccountLog103() {
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to initialize correctly", dod.getRandomUserAccountLog103());
        UserAccountLog103 obj = dod.getNewTransientUserAccountLog103(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserAccountLog103' identifier to be null", obj.getLogId());
        userAccountLogService.saveUserAccountLog103(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserAccountLog103' identifier to no longer be null", obj.getLogId());
    }
    
    @Test
    public void UserAccountLog103IntegrationTest.testDeleteUserAccountLog103() {
        UserAccountLog103 obj = dod.getRandomUserAccountLog103();
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to initialize correctly", obj);
        String id = obj.getLogId();
        Assert.assertNotNull("Data on demand for 'UserAccountLog103' failed to provide an identifier", id);
        obj = userAccountLogService.findUserAccountLog103(id);
        userAccountLogService.deleteUserAccountLog103(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserAccountLog103' with identifier '" + id + "'", userAccountLogService.findUserAccountLog103(id));
    }
    
}
