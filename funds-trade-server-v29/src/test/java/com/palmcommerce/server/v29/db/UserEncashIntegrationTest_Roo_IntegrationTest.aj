// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserEncash;
import com.palmcommerce.server.v29.db.UserEncashDataOnDemand;
import com.palmcommerce.server.v29.db.UserEncashIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserEncashIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserEncashIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserEncashIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserEncashIntegrationTest: @Transactional;
    
    @Autowired
    UserEncashDataOnDemand UserEncashIntegrationTest.dod;
    
    @Test
    public void UserEncashIntegrationTest.testCountUserEncashes() {
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to initialize correctly", dod.getRandomUserEncash());
        long count = UserEncash.countUserEncashes();
        Assert.assertTrue("Counter for 'UserEncash' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserEncashIntegrationTest.testFindUserEncash() {
        UserEncash obj = dod.getRandomUserEncash();
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to initialize correctly", obj);
        String id = obj.getEncashId();
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to provide an identifier", id);
        obj = UserEncash.findUserEncash(id);
        Assert.assertNotNull("Find method for 'UserEncash' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserEncash' returned the incorrect identifier", id, obj.getEncashId());
    }
    
    @Test
    public void UserEncashIntegrationTest.testFindAllUserEncashes() {
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to initialize correctly", dod.getRandomUserEncash());
        long count = UserEncash.countUserEncashes();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserEncash', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserEncash> result = UserEncash.findAllUserEncashes();
        Assert.assertNotNull("Find all method for 'UserEncash' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserEncash' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserEncashIntegrationTest.testFindUserEncashEntries() {
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to initialize correctly", dod.getRandomUserEncash());
        long count = UserEncash.countUserEncashes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserEncash> result = UserEncash.findUserEncashEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserEncash' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserEncash' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserEncashIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to initialize correctly", dod.getRandomUserEncash());
        UserEncash obj = dod.getNewTransientUserEncash(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserEncash' identifier to be null", obj.getEncashId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserEncash' identifier to no longer be null", obj.getEncashId());
    }
    
    @Test
    public void UserEncashIntegrationTest.testRemove() {
        UserEncash obj = dod.getRandomUserEncash();
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to initialize correctly", obj);
        String id = obj.getEncashId();
        Assert.assertNotNull("Data on demand for 'UserEncash' failed to provide an identifier", id);
        obj = UserEncash.findUserEncash(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserEncash' with identifier '" + id + "'", UserEncash.findUserEncash(id));
    }
    
}
