// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.palmcommerce.server.v29.db;

import com.palmcommerce.server.v29.db.UserAccountLog109;
import com.palmcommerce.server.v29.db.UserAccountLog109DataOnDemand;
import com.palmcommerce.server.v29.service.UserAccountLogService;
import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect UserAccountLog109DataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserAccountLog109DataOnDemand: @Component;
    
    private Random UserAccountLog109DataOnDemand.rnd = new SecureRandom();
    
    private List<UserAccountLog109> UserAccountLog109DataOnDemand.data;
    
    @Autowired
    UserAccountLogService UserAccountLog109DataOnDemand.userAccountLogService;
    
    public UserAccountLog109 UserAccountLog109DataOnDemand.getNewTransientUserAccountLog109(int index) {
        UserAccountLog109 obj = new UserAccountLog109();
        setCash(obj, index);
        setCreateTime(obj, index);
        setForigenId(obj, index);
        setFrezzMoney(obj, index);
        setOptCode(obj, index);
        setOptMoney(obj, index);
        setPartnerId(obj, index);
        setUserId(obj, index);
        return obj;
    }
    
    public void UserAccountLog109DataOnDemand.setCash(UserAccountLog109 obj, int index) {
        BigDecimal cash = BigDecimal.valueOf(index);
        if (cash.compareTo(new BigDecimal("9999999999999.99")) == 1) {
            cash = new BigDecimal("9999999999999.99");
        }
        obj.setCash(cash);
    }
    
    public void UserAccountLog109DataOnDemand.setCreateTime(UserAccountLog109 obj, int index) {
        Date createTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreateTime(createTime);
    }
    
    public void UserAccountLog109DataOnDemand.setForigenId(UserAccountLog109 obj, int index) {
        String forigenId = "forigenId_" + index;
        if (forigenId.length() > 25) {
            forigenId = forigenId.substring(0, 25);
        }
        obj.setForigenId(forigenId);
    }
    
    public void UserAccountLog109DataOnDemand.setFrezzMoney(UserAccountLog109 obj, int index) {
        BigDecimal frezzMoney = BigDecimal.valueOf(index);
        if (frezzMoney.compareTo(new BigDecimal("9999999999999.99")) == 1) {
            frezzMoney = new BigDecimal("9999999999999.99");
        }
        obj.setFrezzMoney(frezzMoney);
    }
    
    public void UserAccountLog109DataOnDemand.setOptCode(UserAccountLog109 obj, int index) {
        String optCode = "optCode_" + index;
        if (optCode.length() > 30) {
            optCode = optCode.substring(0, 30);
        }
        obj.setOptCode(optCode);
    }
    
    public void UserAccountLog109DataOnDemand.setOptMoney(UserAccountLog109 obj, int index) {
        BigDecimal optMoney = BigDecimal.valueOf(index);
        if (optMoney.compareTo(new BigDecimal("9999999999999.99")) == 1) {
            optMoney = new BigDecimal("9999999999999.99");
        }
        obj.setOptMoney(optMoney);
    }
    
    public void UserAccountLog109DataOnDemand.setPartnerId(UserAccountLog109 obj, int index) {
        String partnerId = "partnerId_" + index;
        if (partnerId.length() > 30) {
            partnerId = partnerId.substring(0, 30);
        }
        obj.setPartnerId(partnerId);
    }
    
    public void UserAccountLog109DataOnDemand.setUserId(UserAccountLog109 obj, int index) {
        String userId = "userId_" + index;
        if (userId.length() > 30) {
            userId = userId.substring(0, 30);
        }
        obj.setUserId(userId);
    }
    
    public UserAccountLog109 UserAccountLog109DataOnDemand.getSpecificUserAccountLog109(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserAccountLog109 obj = data.get(index);
        String id = obj.getLogId();
        return userAccountLogService.findUserAccountLog109(id);
    }
    
    public UserAccountLog109 UserAccountLog109DataOnDemand.getRandomUserAccountLog109() {
        init();
        UserAccountLog109 obj = data.get(rnd.nextInt(data.size()));
        String id = obj.getLogId();
        return userAccountLogService.findUserAccountLog109(id);
    }
    
    public boolean UserAccountLog109DataOnDemand.modifyUserAccountLog109(UserAccountLog109 obj) {
        return false;
    }
    
    public void UserAccountLog109DataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = userAccountLogService.findUserAccountLog109Entries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserAccountLog109' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserAccountLog109>();
        for (int i = 0; i < 10; i++) {
            UserAccountLog109 obj = getNewTransientUserAccountLog109(i);
            try {
                userAccountLogService.saveUserAccountLog109(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
